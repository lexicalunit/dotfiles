#!/bin/bash

# Delete all docker containers and images.

if echo "$*" | egrep -q -- "--help|-h"; then
    echo "Delete all docker containers and images." >&2
    exit 0
fi

# Delete all containers
docker rm $(docker ps -a -q)

# Delete all images
docker rmi $(docker images -q)
