#!/bin/bash

# Installs everything to ~/bin.

mkdir -p ~/bin

for I in *; do
	if [[ "$I" != "install" && "$I" != "update_bin" && "$I" != "usages" ]]; then
		rsync -avl $I ~/bin/$I
	fi
done

echo "~/bin install complete!"
